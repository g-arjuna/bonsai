# CV7 validation result template

This is the shape `scripts/ops/rebuild_and_validate.sh` writes for each run.
File name: `docs/test_results/cv7-validation-<UTC-ISO>.md`.

After every Ubuntu validation iteration the script commits + pushes the
populated file so the Mac side picks it up via `git pull origin main` and
plans the next iteration from concrete evidence.

---

# CV7 validation run — YYYY-MM-DDThhmmZ

Generated by `scripts/ops/rebuild_and_validate.sh`. Each section corresponds
to one validation step; PASS/FAIL is the line in **bold**.

## 1. git pull origin main

    $ git fetch origin main
    $ git reset --hard origin/main
**PASS**: at <sha>

## 2. regenerate Python gRPC stubs

    $ .venv/bin/python python/gen_protos.py
**PASS**: protos regenerated

## 3. cargo build --release

(tail of build output)
**PASS**: cargo build succeeded

## 4. cargo test sidecar_registry (T4-2 unit tests)

(test output — should show 9 passing tests)
**PASS**: sidecar_registry tests pass

## 5. teardown + start bonsai with sidecar

(teardown output, startup wrapper output)

## 6. /api/sidecars after startup

```json
{"sidecars": [...], "required_kinds": ["rules"], "missing_required": null}
```
**OBSERVED**: N sidecar(s) registered

## 7. wait 20s for first heartbeat then re-probe

```json
{"sidecars": [{"name": "rules-local", "kind": "rules", "status": "healthy", "events_in_total": ..., "detections_out_total": ..., ...}], ...}
```
**PASS**: 1 sidecar(s) healthy

## 8. /health (BONSAI_REQUIRE_SIDECAR=rules already set)

```json
{"status": "ok"}
```
**PASS**: /health = ok

## 9. bonpy UI served at /bonpy/

**PASS**: GET /bonpy/ returned 200

## 10. degrade probe (kill sidecar; /health should flip to degraded after stale window)

```
killed sidecar pid <pid> — waiting 130s for lost-status window
HTTP 503  body: {"status":"degraded","missing_required_sidecars":["rules"]}
```
**PASS**: /health correctly degraded after sidecar loss

## 11. teardown

**PASS**: teardown clean

## Summary

- PASS: 9
- WARN: 0
- FAIL: 0

**Verdict**: T4-1 through T4-6 validated. Tier 4 is safe to close pending T4-7 (delete src/event_detection.rs).

## 12. push results

(git commit + push output)
