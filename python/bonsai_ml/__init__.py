"""bonsai_ml — graph-position embeddings and ML feature schema versioning.

Modules:
  embeddings    — spectral graph embedding computation + push to core API (T2-1)
  feature_schema — FeatureSchema versioning contract used by GNN training (T7-11)
"""
