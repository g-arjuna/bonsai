# EV1 Testing Run 2 — 2026-05-28

## Test Environment Sanity Check

- **Local Time**: 2026-05-28 20:25
- **Bonsai Core**: Running on `http://localhost:3000` (mode=all, run_core=true, run_collector=true).
- **Sidecar Process**: Running under PID `1595069` using virtualenv Python.

---

## 1. Issue Encountered: ML Job Scheduling Silent Failure & Serialization Error

### Symptoms
1. **No Automatic Job/Embedding Trigger**: Automatic interval/cron jobs like `syslog_embedding` or `config_embedding` are never triggered. The ML job run history is empty or contains no new runs.
2. **`gnn_snapshot` Shows Interrupted**: On Core startup, existing running/pending runs are cleaned up and set to `status='interrupted'`. However, they are never scheduled or run again, remaining permanently stuck in the interrupted state.
3. **404 / Missing Models**: Since no training/export/GNN snapshot jobs are triggered automatically, no `ModelArtifact` records exist, causing endpoints (like model active models) to return 404 or empty.

### Root Cause Analysis (RCA)
- **APScheduler 4.x SQLAlchemy Backend**: The sidecar uses `SQLAlchemyDataStore` with an SQLite database (`runtime/ml_jobs.db`). In APScheduler 4.x, schedules stored in a database backend must be fully serializable.
- **Non-Serializable Lambda Functions**: In [python/bonsai_ml/job_engine.py](file:///home/arjuna/Desktop/bonsai/python/bonsai_ml/job_engine.py#L364-L372), the scheduler adds schedules using a lambda trigger:
  ```python
  await self._scheduler.add_schedule(
      lambda jid=job_id: asyncio.ensure_future(self._run_job(jid)),
      trigger,
      id=job_id,
      conflict_policy="replace",
  )
  ```
- **Silent Debug Invisibility**: Since python lambda functions are not importable, APScheduler fails to serialize them and throws a `SerializationError`. This is caught by `except Exception as exc` in `_register_schedule` and logged as a `DEBUG` level log:
  ```python
  log.debug("Could not register schedule for %s: %s", job_id, exc)
  ```
  Because the logging level defaults to `WARNING` or `INFO`, this failure was completely silent, leaving APScheduler running but scheduling absolutely nothing.

---

## 2. Implemented Fixes (Session 3)

The following changes were applied to [python/bonsai_ml/job_engine.py](file:///home/arjuna/Desktop/bonsai/python/bonsai_ml/job_engine.py):

### A. Global Engine Reference and Module-Level Function
Defined a module-level variable `_global_engine` and a serializable module-level trigger function `_trigger_job_from_scheduler` that calls the active engine's `_run_job`:
```python
_global_engine: Optional[BonsaiJobEngine] = None

async def _trigger_job_from_scheduler(job_id: str) -> None:
    """Module-level serializable function for APScheduler 4.x.
    Calls the global engine instance to run the job.
    """
    global _global_engine
    if _global_engine:
        await _global_engine._run_job(job_id)
    else:
        logging.getLogger(__name__).error(
            "JobEngine: _trigger_job_from_scheduler called but _global_engine is not set"
        )
```

### B. Store Instance Reference on Init
Set `_global_engine = self` in `BonsaiJobEngine.__init__`.

### C. Update Schedule Registration to Pass Arguments
Replaced the lambda function with `_trigger_job_from_scheduler` and passed `job_id` via `args`:
```python
            await self._scheduler.add_schedule(
                _trigger_job_from_scheduler,
                trigger,
                id=job_id,
                args=[job_id],
                conflict_policy="replace",
            )
```

### D. Elevate Registration Failures Log Level
Elevated the exception log level from `DEBUG` to `WARNING` to ensure any future schedule registration issues are immediately visible in the logs:
```python
        except Exception as exc:
            log.warning("JobEngine: Could not register schedule for %s: %s", job_id, exc)
```

---

## 3. Verification Plan

1. **Restart Bonsai Core and Sidecar**:
   - Restart the Bonsai Core daemon.
   - Confirm the Python sidecar is spawned, initialization of APScheduler succeeds, and log outputs show successful schedule registrations.
2. **Verify SQLite Schedule Registry**:
   - Verify that schedules are populated in `runtime/ml_jobs.db`.
3. **Verify Automated Triggers**:
   - Wait 60s for the `syslog_embedding` schedule to trigger.
   - Check if new runs appear in the ML jobs history and if embeddings are successfully generated.
