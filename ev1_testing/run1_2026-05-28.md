# EV1 Testing Run 1 — 2026-05-28

## Test Environment Sanity Check

- **Local Time**: 2026-05-28 18:05
- **Git Commit**: `d59f444d3edeb209b338fea78d7593aa063cbf33` (main branch)
- **ContainerLab Topology (`bonsai-srl`)**: Up and healthy
  - `srl1` (`172.100.100.11`): Reachable (Ping OK)
  - `srl2` (`172.100.100.12`): Reachable (Ping OK)
  - `srl3` (`172.100.100.13`): Reachable (Ping OK)
- **Bonsai Core**: Running on `http://localhost:3000` (mode=all, run_core=true, run_collector=true).

---

## 1. Issue Encountered: Managed Sidecar Startup Flakiness & Stale Status

### Symptoms
1. On initial boot of Bonsai core, the python sidecar child is spawned (indicated in logs) but is immediately terminated.
2. The endpoint `GET /api/sidecar/status` shows no registered sidecars: `{"sidecars":[]}`.
3. The endpoint `GET /api/sidecar/process-status` reports that the process is still running with a stale PID (e.g. `1403924`):
   ```json
   {"state":"running","pid":1403924,"restart_count":0,"last_exit_code":null,"uptime_secs":51}
   ```
4. Calling `POST /api/sidecar/start` or `POST /api/sidecar/restart` starts the python process, but the process runs without a background supervisor loop (if it crashes, it will not restart).

### Root Cause Analysis (RCA)
- **Immediate Sender Drop**: In [src/server_startup.rs](file:///home/arjuna/Desktop/bonsai/src/server_startup.rs#L1596), the supervisor's watch channel is instantiated as:
  ```rust
  let (_, shutdown_rx) = tokio::sync::watch::channel(false);
  ```
  Because the sender is bound to `_`, it is immediately dropped by the compiler, closing the watch channel.
- **Premature Supervisor Exit**: Inside `run_supervised` in [src/sidecar_manager.rs](file:///home/arjuna/Desktop/bonsai/src/sidecar_manager.rs#L146), the loop selects on `shutdown_rx.changed()`. Because the channel is closed, `changed()` resolves immediately.
- **Silent Termination**: The supervisor executes its shutdown arm, calling `c.start_kill()` on the child process, and returns. This kills the auto-started sidecar immediately on boot and drops supervision.
- **Stale State**: Because the supervisor loop exits via `return` without updating `self.inner`, the status in the manager's memory remains permanently stuck in the `Running` state with the stale `pid`.

---

## 2. Suggestions for Implementation (Developer Task)

To solve this supervision and lifecycle gap, the following modifications are recommended:

### A. Move Supervision to `SidecarProcessManager`
Instead of manually spawning the supervisor task in `server_startup.rs` and passing a temporary `shutdown_rx`, make `start()` self-contained so that it subscribes to its own persistent `shutdown_tx` and spawns `run_supervised` automatically.

Modify `SidecarProcessManager::start` in `src/sidecar_manager.rs`:
```rust
    pub async fn start(self: &Arc<Self>) -> Result<()> {
        let mut guard = self.inner.lock().await;
        if guard.state == SidecarProcessState::Running
            || guard.state == SidecarProcessState::Starting
        {
            return Err(anyhow::anyhow!("sidecar is already running (pid {:?})", guard.pid));
        }
        guard.state = SidecarProcessState::Starting;
        drop(guard);
        self.spawn_child().await?;

        // Automatically spawn the supervisor task using the persistent shutdown_tx channel
        let shutdown_rx = self.shutdown_tx.subscribe();
        Arc::clone(self).run_supervised(shutdown_rx);
        Ok(())
    }
```

### B. Fix `stop()` to Signal by PID when Child Handle is Taken
Since the supervisor loop takes the child process handle out of the lock (`guard.child.take()`), the `stop()` method finds `guard.child` is `None` and fails to terminate it. We should use the stored `pid` to send a `SIGTERM` (signal 15) using the system `kill` command.

Modify `SidecarProcessManager::stop` in `src/sidecar_manager.rs`:
```rust
    pub async fn stop(&self) -> Result<()> {
        let mut guard = self.inner.lock().await;
        match guard.state {
            SidecarProcessState::Running | SidecarProcessState::Starting => {
                guard.state = SidecarProcessState::Stopping;
                if let Some(child) = guard.child.as_mut() {
                    child.start_kill().context("SIGTERM to sidecar")?;
                    info!("sent SIGTERM to sidecar (pid {:?})", guard.pid);
                } else if let Some(pid) = guard.pid {
                    // Child handle taken by supervisor loop; send SIGTERM via PID
                    let mut kill_cmd = tokio::process::Command::new("kill");
                    kill_cmd.arg("-15").arg(pid.to_string());
                    match kill_cmd.status().await {
                        Ok(status) if status.success() => {
                            info!("sent SIGTERM to sidecar via PID {:?}", pid);
                        }
                        Ok(status) => {
                            warn!("failed to send SIGTERM to sidecar PID {:?}: exit status {:?}", pid, status);
                        }
                        Err(e) => {
                            warn!("failed to execute kill command for sidecar PID {:?}: {:?}", pid, e);
                        }
                    }
                }
                Ok(())
            }
            _ => Err(anyhow::anyhow!("sidecar is not running")),
        }
    }
```

### C. Improve `restart()` Polling
In `src/sidecar_manager.rs`, modify `restart()` to wait/poll for the state to transition to `Stopped` (to ensure the old process and supervisor have cleanly exited) before launching `self.start()` again.
```rust
    pub async fn restart(self: &Arc<Self>) -> Result<()> {
        let _ = self.stop().await;
        // Wait for the state to transition to Stopped (up to 5 seconds)
        for _ in 0..50 {
            {
                let guard = self.inner.lock().await;
                if guard.state == SidecarProcessState::Stopped {
                    break;
                }
            }
            tokio::time::sleep(Duration::from_millis(100)).await;
        }
        self.start().await
    }
```

### D. Simplify `server_startup.rs`
Remove the manual supervisor spawning.
```rust
                    if cfg.managed_sidecar.auto_start {
                        info!("managed_sidecar.auto_start=true — spawning sidecar process");
                        let _ = mgr.start().await;
                    }
```

---

## Developer Corrective Action — commit `8399c8d`

All four fixes applied in **`src/sidecar_manager.rs`** and **`src/server_startup.rs`**.

### Fix A — `start()` now self-contained (`sidecar_manager.rs`)
- Signature changed from `pub async fn start(&self)` → `pub async fn start(self: &Arc<Self>)`
- After `spawn_child()` succeeds, `start()` calls `self.shutdown_tx.subscribe()` to get a `Receiver`
  from the **persistent** sender (held by `Arc<SidecarProcessManager>`) and immediately calls
  `Arc::clone(self).run_supervised(shutdown_rx)`.
- The sender is never dropped — it lives as long as the `Arc` — so the watch channel never closes spuriously.

### Fix B — `stop()` PID fallback (`sidecar_manager.rs`)
- Added `else if let Some(pid) = guard.pid` branch: when the supervisor has taken the child handle
  out of the mutex (its `guard.child.take()` pattern), `stop()` sends `kill -15 <pid>` via
  `tokio::process::Command::new("kill").arg("-15").arg(pid.to_string()).status().await`.
- Logs success/failure at info/warn level.

### Fix C — `restart()` polls for Stopped (`sidecar_manager.rs`)
- Replaced the `sleep(2s) + spawn_child()` pattern with a 50-iteration × 100 ms poll
  (up to 5 s total) waiting for `guard.state == Stopped` before calling `self.start()`.
- `self.start()` (not `spawn_child()`) is called so the new supervisor loop is also attached.

### Fix D — `server_startup.rs` simplified
- Removed the `let (_, shutdown_rx) = tokio::sync::watch::channel(false)` and the
  `Arc::clone(&mgr).run_supervised(shutdown_rx)` call.
- Boot path is now just: `if cfg.managed_sidecar.auto_start { mgr.start().await }`.

### Verification steps
After `git pull && cargo build --release`:
1. Start Bonsai with `[managed_sidecar] auto_start = true` in `bonsai.toml`.
2. Confirm sidecar spawns and registers healthy: `curl localhost:9292/health`.
3. Confirm `GET /api/sidecar/process-status` shows `state: "running"` with correct PID.
4. `POST /api/sidecar/stop` → state should transition to `stopping` → `stopped`.
5. `POST /api/sidecar/start` → state `starting` → `running`, new PID, supervisor re-attached.
6. `POST /api/sidecar/restart` → old process SIGTERMed, polls for stop, new process starts.
7. Kill the sidecar externally (`kill -9 <pid>`) — supervisor should restart it after backoff delay.

---

## 3. Next Steps for Testing — Sidecar Lifecycle

1. Once devs implement the fixes above and commit/push, we will pull the code.
2. Rebuild core with `cargo build --release`.
3. Start Bonsai and verify:
   - Auto-start works reliably and the sidecar immediately registers as `healthy`.
   - On-demand Stop, Start, and Restart API endpoints successfully manage both the process and its supervisor loop.
   - Verify multi-source syslog + SNMP correlation flows.

---

## 4. Issue Encountered: ML Dashboard — Jobs Piling Up / Models 404 / Embeddings Never Ran

### Symptoms
- Dashboard: **Sidecar healthy** but **Last GNN —**, **Last Export —**
- Jobs page: `gnn_snapshot` and `graph_snapshot` in state `running` indefinitely — never complete — accumulating with every scheduler cycle
- Models page: **404 / empty** — no `ModelArtifact` records
- Parquet exports page: **empty** — no `ParquetExport` records
- GNN inference results page: **empty**
- Embeddings: `syslog_embedding` never fired; `config_embedding` never ran

### Root Cause Analysis

#### Bug 1 — `graph_snapshot` / `gnn_snapshot` not registered in Python job engine
`build_default_engine()` in `python/bonsai_ml/job_engine.py` registers 5 job handlers but is **missing** handlers for `graph_snapshot` and `gnn_snapshot`.

The Rust scheduler fires these jobs on cron (`0 */1 * * *` and `0 */4 * * *`) and creates `MlJobRun` DB records with `status='running'`. The Python engine receives the APScheduler event but has no function registered for these job IDs (`fn = self._job_registry.get(job_id)` returns `None` → logs a warning and returns). The job is never completed → DB records pile up forever in `running` state → UI shows perpetual spinners.

#### Bug 2 — `syslog_embedding` cron never fires from Rust scheduler
The Rust `DEFAULT_SCHEDULES` had `("sched-syslog_embedding", "syslog_embedding", "* * * * *")`.

`cron_should_fire()` in `ml_jobs.rs` only handles two patterns:
- `"*/N"` in the hour field (every N hours)
- a specific hour number (daily/weekly)

`"* * * * *"` has `"*"` in the hour field — this matches **neither** branch, so `cron_should_fire()` always returns `false`. The Rust tick never fires `syslog_embedding`, so the DB never even gets a pending record for Python to pick up. (Python's own APScheduler 60s interval still fires it when the sidecar is healthy — but it was dying on boot.)

#### Bug 3 — Models / Parquet / GNN inference empty (downstream of Bug 1 + sidecar lifecycle)
Models (404) and Parquet exports (empty) are downstream: `anomaly_export_daily` is the job that creates `ParquetExport` records. Since the sidecar was dying on boot (sidecar lifecycle bug from Issue 1), the job engine never ran at all. Now that the sidecar lifecycle is fixed, these should populate after the first successful export run.

GNN inference requires `buffer_size >= 2` in the `SnapshotStore`. With `graph_snapshot` never completing, the snapshot buffer was always empty → inference always returned `None` immediately.

### Corrective Actions — commit `3b76dce`

#### Fix 1 — Register `graph_snapshot` and `gnn_snapshot` in `build_default_engine()` (`job_engine.py`)
Added `_take_graph_snapshot()` async handler:
- Calls `GraphSnapshotClient(base_url=api_url).fetch()` for the live graph
- Builds `HeteroData` via `build_hetero_data(raw)`
- Writes to `SnapshotStore()` (default dir `runtime/parquet/gnn_snapshots/`)
- Logs buffer size and stale status
- Registered as both `graph_snapshot` (every 1h) and `gnn_snapshot` (every 4h) — both point to the same handler since they do the same work

```python
engine.register_job("graph_snapshot", _take_graph_snapshot, "interval", hours=1)
engine.register_job("gnn_snapshot",   _take_graph_snapshot, "interval", hours=4)
```

#### Fix 2 — Fix `syslog_embedding` cron expression in `DEFAULT_SCHEDULES` (`ml_jobs.rs`)
Changed `"* * * * *"` → `"0 */1 * * *"` (every 1h from Rust). Python's own 60s APScheduler interval is still the primary driver; Rust just fires a boot-time nudge.

#### Fix 3 — Stale running-job cleanup at boot (`ml_jobs.rs`)
Added cleanup in `seed_default_ml_schedules()`: any `MlJobRun` with `status='running'` or `status='pending'` at boot time (orphaned from a previous session that crashed before completing) is immediately set to `status='interrupted'` with `completed_at_ns=now` and `error_message='server restarted before job completed'`. Prevents unlimited accumulation.

### Verification Steps
After `git pull && cargo build --release && pip install -e python/`:
1. Restart Bonsai + sidecar. Watch sidecar logs for `graph_snapshot: wrote snapshot to buffer`.
2. After ~1h (or trigger manually): `GET /api/ml/jobs?limit=10` — `graph_snapshot` and `gnn_snapshot` should show `status=succeeded`.
3. After `syslog_embedding` interval: `GET /api/ml/embeddings/stats` — counts should increment.
4. After `gnn_inference` runs with buffer_size ≥ 2: `GET /api/gnn/results` — should return scores.
5. Check dashboard: Last GNN, Last Export timestamps should populate after first successful runs.
6. Verify no perpetual `running` jobs after a Bonsai restart (stale cleanup check).

---

## 5. Next Steps for Testing — ML Pipeline

1. `git pull && cargo build --release` on Ubuntu.
2. `pip install -e python/` to pick up `job_engine.py` changes.
3. Restart Bonsai with `[managed_sidecar] auto_start = true`.
4. Wait 15 min and check: `curl localhost:3000/api/ml/jobs?limit=20 | python3 -m json.tool`
5. Confirm `graph_snapshot` succeeded and `syslog_embedding` ran.
6. Trigger inference manually if needed: POST a `graph_snapshot` job via the Jobs UI.

---

## 6. Tester Verification and Feedback — 2026-05-28

I pulled the fixes from commit `8399c8d` and `3b76dce`, rebuilt the codebase (`cargo build --release`), and reinstalled the python package (`pip install -e python/`). The following results were verified:

### A. Sidecar Lifecycle & Supervision (Status: SOLVED)
- **Auto-start**: On boot, the sidecar is spawned (`pid 1453564`) and immediately registers as `healthy` / `rules-local` with its health endpoint reachable. The boot-time flakiness is fully resolved.
- **Process Status Endpoint**: `GET /api/sidecar/process-status` successfully returns the correct supervisor status (`state: "running"`, the active PID, and `restart_count: 0`).
- **Stop Endpoint**: `POST /api/sidecar/stop` transitions the state to `stopped`, clears the PID, and terminates the child process cleanly with exit code `0`, verifying the `kill -15` PID fallback.
- **Start Endpoint**: `POST /api/sidecar/start` transitions the state back to `running`, spawns a new PID, and successfully re-attaches process supervision.

### B. ML Pipeline & Job Engine (Status: SOLVED)
- **Stale Job Cleanup at Boot**: Verified that on startup, the scheduler identifies and cleans up stale/pending/running runs from the previous crash:
  `2026-05-28T13:09:20.075772Z  INFO bonsai::http_server::ml_jobs: seed_default_ml_schedules: cleaned up stale running/pending jobs`
- **Default Schedule Seeding**: Verified that the 7 default ML schedules are correctly initialized:
  `2026-05-28T13:09:20.135918Z  INFO bonsai::http_server::ml_jobs: seeded default ML schedule: id=sched-syslog_embedding cron=0 */1 * * *`
- **Job Handler Registration**: The sidecar starts up APScheduler cleanly and registers the `graph_snapshot` and `gnn_snapshot` job handlers.

Since some key ML scheduler issues are still pending, we are keeping this run open for further corrective actions.

---

## 7. Tester Diagnostic Update (ML Schedules/Embeddings Not Running) — 2026-05-28

### Symptoms
- The ML Job runs history is completely empty (`{"jobs":[]}` from `GET /api/ml/jobs`).
- No embeddings or GNN snapshot jobs are being run/scheduled.

### Root Cause Analysis

#### Bug A: Python Environment contains APScheduler 3.x (Instead of 4.x)
- **RCA**: The codebase in [python/bonsai_ml/job_engine.py](file:///home/arjuna/Desktop/bonsai/python/bonsai_ml/job_engine.py#L262-L264) is written for **APScheduler 4.x** (which is a complete rewrite using `AsyncScheduler`, `SQLAlchemyDataStore`, and `LocalEventBroker`).
- However, the virtualenv has standard **APScheduler 3.11.2** installed. In APScheduler 3.x, these modules/classes do not exist.
- This causes a silent `ImportError` on startup, falling back to the `_InMemoryFallbackScheduler` which disables all automated interval scheduling.
- **RCA Downstream Deps**: Upgrading to APScheduler 4.x also requires `aiosqlite`, `sniffio`, and `greenlet` (required by `sqlalchemy[asyncio]`).

#### Bug B: JSON dictionary iteration bug in `_load_schedules_from_api()`
- **RCA**: In `job_engine.py` at line 294, `_load_schedules_from_api` does:
  ```python
  schedules = await resp.json()
  for sched in schedules:
  ```
  Since `GET /api/ml/schedules` returns `{"schedules": [...]}` (a dictionary), `for sched in schedules` iterates over the keys of the dictionary (which is only the string `"schedules"`).
  It then tries to run `sched.get("enabled")`, raising `AttributeError: 'str' object has no attribute 'get'`.
  This is caught by the `except Exception` block and silently ignored, so no schedules are ever registered.

---

## 8. Suggestions for Developer Actions

### 1. Upgrade virtualenv packages
Run the following in the virtualenv to install APScheduler 4.x and its required concurrency/database backends:
```bash
.venv/bin/pip install "apscheduler>=4.0.0a1" aiosqlite "sqlalchemy[asyncio]" sniffio
```

### 2. Fix dictionary iteration in `job_engine.py`
Modify `_load_schedules_from_api` in `python/bonsai_ml/job_engine.py`:
```diff
     async def _load_schedules_from_api(self) -> None:
         """Read MlJobSchedule records from /api/ml/schedules and register them."""
         try:
             import aiohttp
             async with aiohttp.ClientSession() as session:
                 async with session.get(
                     f"{self.api_url}/api/ml/schedules", timeout=aiohttp.ClientTimeout(total=10)
                 ) as resp:
                     if resp.ok:
                         schedules = await resp.json()
-                        for sched in schedules:
+                        for sched in schedules.get("schedules", []):
                             if sched.get("enabled") and sched.get("job_id") in self._job_registry:
                                 await self._register_schedule(sched)
```

