# srl-leaf4 — Signal-Test Lab leaf 4
# Role     : IS-IS L2, iBGP RR-client, SNMP trap source (spine2 only — single-homed)
# Loopback : 10.9.0.7/32
# e1-1     : 10.9.12.5/31  to srl-spine2 e1-4  (leaf4=.5, spine2=.4)
# SNMP     : sends traps to 172.100.109.1:9162
#            Single-homed: ideal fault target (total isolation on uplink failure)

# ── Loopback ──────────────────────────────────────────────────────────────────
set / interface lo0 subinterface 0 ipv4 admin-state enable address 10.9.0.7/32

# ── Uplink to spine2 ─────────────────────────────────────────────────────────
set / interface ethernet-1/1 admin-state enable description "to srl-spine2 e1-4"
set / interface ethernet-1/1 subinterface 0 ipv4 admin-state enable address 10.9.12.5/31
set / interface ethernet-1/1 subinterface 0 ipv6 admin-state enable

# ── Default network instance ──────────────────────────────────────────────────
set / network-instance default interface lo0.0
set / network-instance default interface ethernet-1/1.0

# ── IS-IS Level 2 ─────────────────────────────────────────────────────────────
set / network-instance default protocols isis instance main admin-state enable
set / network-instance default protocols isis instance main level-capability L2
set / network-instance default protocols isis instance main net [ 49.0009.0100.0900.0007.00 ]
set / network-instance default protocols isis instance main ipv4-unicast admin-state enable
set / network-instance default protocols isis instance main interface lo0.0 passive true
set / network-instance default protocols isis instance main interface ethernet-1/1.0 circuit-type point-to-point

# ── iBGP AS 65900 — RR client ─────────────────────────────────────────────────
set / network-instance default protocols bgp afi-safi ipv4-unicast admin-state enable
set / network-instance default protocols bgp admin-state enable
set / network-instance default protocols bgp autonomous-system 65900
set / network-instance default protocols bgp router-id 10.9.0.7

set / network-instance default protocols bgp group ibgp
set / network-instance default protocols bgp group ibgp peer-as 65900
set / network-instance default protocols bgp group ibgp afi-safi ipv4-unicast admin-state enable
set / network-instance default protocols bgp group ibgp transport local-address 10.9.0.7
set / network-instance default protocols bgp group ibgp failure-detection enable-bfd true

set / network-instance default protocols bgp neighbor 10.9.0.1 peer-group ibgp
set / network-instance default protocols bgp neighbor 10.9.0.1 description "srl-super1 (RR)"

# ── SNMP — send traps to bonsai host ──────────────────────────────────────────
# bonsai SNMP receiver listens on 0.0.0.0:9162
# SRL v26.3.1 SNMP schema: network-instance/access-group/trap-group
set / system snmp network-instance mgmt admin-state enable
set / system snmp access-group snmpv2c admin-state enable
set / system snmp access-group snmpv2c security-level no-auth-no-priv
set / system snmp access-group snmpv2c community-entry ce1 community bonsai-test
set / system snmp trap-group bonsai admin-state enable
set / system snmp trap-group bonsai network-instance mgmt
set / system snmp trap-group bonsai destination bonsai-host address 172.100.109.1
set / system snmp trap-group bonsai destination bonsai-host port 9162
set / system snmp trap-group bonsai destination bonsai-host security-level no-auth-no-priv
set / system snmp trap-group bonsai destination bonsai-host community-entry bonsai community bonsai-test

# ── BFD on uplink ─────────────────────────────────────────────────────────────
set / bfd subinterface ethernet-1/1.0 admin-state enable
set / bfd subinterface ethernet-1/1.0 desired-minimum-transmit-interval 1000000
set / bfd subinterface ethernet-1/1.0 required-minimum-receive 1000000
set / bfd subinterface ethernet-1/1.0 detection-multiplier 3
